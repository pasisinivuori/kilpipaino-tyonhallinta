update public.toimeksiannot
   set valmistumispaiva = nullif({{ ajPvm.formattedDate }}::text, '')::date
 where id = nullif({{ appsmith.store.ajTyo.id || '' }}::text, '')::uuid
returning id, tp_numero, valmistumispaiva;